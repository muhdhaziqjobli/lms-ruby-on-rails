<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<div>
  <%= link_to "Edit", edit_article_path(@article), class: 'button' %></li>
  <%= link_to "Delete", article_path(@article), class: 'button', data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></li>
  <%= link_to "Back", articles_path, class: 'button' %>
</div>

